/**
 * Returns a string value "bar"
 *
 * This function is a simple example that demonstrates basic TypeScript function syntax.
 * It returns a literal string type "bar" as specified in the return type annotation.
 *
 * @returns {string} The string "bar"
 *
 * @example
 * ```typescript
 * const result = foo(); // "bar"
 * console.log(result); // "bar"
 * ```
 */
export function foo(): "bar" {
  return "bar";
}
